<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_sendNotification"
        android:text="Send Notification"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/tv_sendNotification_progress"
        android:layout_below="@+id/tv_sendNotification"
        android:layout_marginTop="20dp"
        android:text="Send Notification progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/tv_sendNotification_bigView"
        android:layout_below="@+id/tv_sendNotification_progress"
        android:layout_marginTop="20dp"
        android:text="Send Notification bigView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <TextView
        android:id="@+id/tv_sendNotification_customView"
        android:layout_below="@+id/tv_sendNotification_bigView"
        android:layout_marginTop="20dp"
        android:text="Send Notification customView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</RelativeLayout>
